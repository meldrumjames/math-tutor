import { EQUATION } from "../../data/dummy-data";

const initialState = {
  equation: EQUATION,
};

const equationReducer = (state = initialState, action) => {
  return state;
};

export default equationReducer;
